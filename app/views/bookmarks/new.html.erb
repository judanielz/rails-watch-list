
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie, collection: Movie.all, label_method: :title, value_method: :id, prompt: "Sélectionnez un film" %>
  <%= f.input :comment %>
  <%= f.button :submit, "Ajouter ce film à la liste" %>
<% end %>
